# Docker Environment Variables Template

# For use with docker-compose
# Copy this file to .env and fill in your actual values

# Node Environment
NODE_ENV=development

# Server Configuration
PORT=8000
BASE_URL=http://backend:8000

# MongoDB (docker-compose uses: mongodb://mongodb:27017/cleanease)
# For local development, you might override it
DATABASE_URL=mongodb://mongodb:27017/cleanease

# Redis (docker-compose uses: redis://redis:6379)
REDIS_URL=redis://redis:6379

# JWT Configuration
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change-this-to-your-actual-secret-key-minimum-32-chars-REQUIRED
JWT_EXPIRY=24h
OTP_EXPIRY=300

# Email Configuration (Gmail SMTP)
# Use App-specific password from Gmail: https://myaccount.google.com/apppasswords
EMAIL=your-email@gmail.com
PASSWORD=your-app-specific-password

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# CORS Configuration
CORS_ORIGIN=http://localhost:5173,http://frontend:3000,https://yourdomain.com

# Rate Limiting
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX_REQUESTS=100
